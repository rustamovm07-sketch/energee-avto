fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
psycopg2-binary==2.9.9
pydantic==2.9.2
pydantic-settings==2.5.2
# Required by pydantic.EmailStr (used in user schemas).
email-validator==2.2.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# bcrypt is pinned: passlib 1.7.4 is incompatible with bcrypt >= 4.1
# (it reads the removed `bcrypt.__about__.__version__` attribute).
bcrypt==4.0.1
python-multipart==0.0.9
python-dotenv==1.0.1
alembic==1.13.2
