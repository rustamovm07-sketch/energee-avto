[pytest]
; Pytest configuration for the Avtosalon backend test-suite.
; Run from the backend/ directory with:  pytest -v
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra -q
filterwarnings =
    ignore::DeprecationWarning
